import { Box } from "@mui/material"

export const TransactionsControl = ({ ...props }) => {
    return (
        <Box {...props}>
            <Box className="border rounded-xl h-70 w-120">
                test
            </Box>
        </Box>
    )
}