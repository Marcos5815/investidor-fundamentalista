import { Box } from "@mui/material"

export const CashFlow = ({ ...props }) => {
    return( 
        <Box { ...props }>
            <Box className="border rounded-xl h-160 w-95">
                Test
            </Box>
        </Box>
    )
}