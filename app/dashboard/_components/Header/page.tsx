import { Box, Typography } from "@mui/material"

export const Header = () => {
    return (
        <Box className='border w-400 pl-4 py-2 rounded-xl'>
            <Typography variant="h4">Dashboard</Typography>
        </Box>
    )
}